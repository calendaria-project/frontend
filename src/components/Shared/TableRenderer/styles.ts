import { createUseStyles } from "react-jss";

export default createUseStyles({
    container: {
        width: "100% !important",
        height: "100%",
        padding: "20px",
        display: "flex",
        justifyContent: "center",
        overflow: "hidden"
    }
});
